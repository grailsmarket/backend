{
  "name": "@grails/shared",
  "version": "1.0.0",
  "description": "Shared libraries and types for Grails backend",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit",
    "migrate": "npx tsx src/db/migrate.ts",
    "seed": "npx tsx src/db/seed.ts"
  },
  "dependencies": {
    "pg": "^8.11.3",
    "redis": "^4.6.12",
    "@elastic/elasticsearch": "^8.11.0",
    "viem": "^2.7.0",
    "zod": "^3.22.4",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "@types/pg": "^8.10.9"
  }
}